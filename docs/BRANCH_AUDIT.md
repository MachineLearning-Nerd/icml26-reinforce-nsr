# Branch audit — Ol99zoW31J

## Original state

The original repository had exactly one top-level branch, `main`, and no
`orx/*` or experiment branches. Its reachable commits were:

| Original commit | Role |
| --- | --- |
| `0f132e8` | exact-moment reproduction of the three live claims |
| `f6b826c` | GitHub-first publication handoff |

No branch-specific evidence was hidden or discarded.

## Current state

The published repository retains exactly one top-level branch:

- `main` — the README, source/claim/branch/gate documentation, clean-room
  verifier, focused tests, cumulative gate, and publication gate.

All reachable commits in this top-level repository are normalized to:

`MachineLearning-Nerd <MachineLearning-Nerd@users.noreply.github.com>`

`main` is the default branch. No old `orx/*` remote references are expected
after publication.
